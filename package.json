{
  "name": "@sidelobe/website",
  "private": true,
  "scripts": {
    "check": "node scripts/validate.mjs && node --check public/app.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler preview"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
